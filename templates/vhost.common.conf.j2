
<Directory {{ item.documentroot }}>
    <IfModule mod_authz_core.c>
        Require all granted
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order allow,deny
        Allow from all
    </IfModule>
</Directory>

{% if item.raw is defined -%}
{{ item.raw }}
{%- endif %}
